function createHTMLOptions() {
  const formHTML = `<%= j render 'components/ticket_category_form', ticket_category: TicketCategory.new %>`;
  document
    .getElementById("addTicketCategory")
    .insertAdjacentHTML('beforebegin', formHTML);
};

createHTMLOptions();

